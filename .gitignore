# This folder IS the public GitHub Pages repo for highvelocity.world.
# Private business material (financials, IP, notes) lives in the PARENT hvl/ folder,
# which is deliberately outside this repo — so only website files can ever be committed.

node_modules/
.DS_Store
Thumbs.db
.env
